@model IEnumerable<SEG.Domain.Models.Aplicacao.FormularioModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Lista de Formulários</h1>

<p>
    <a asp-action="Create">Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id=item.FormularioId }) |
                @Html.ActionLink("Detalhes", "Details", new { id=item.FormularioId }) |
                @Html.ActionLink("Excluir", "Delete", new { id=item.FormularioId })
            </td>
        </tr>
}
    </tbody>
</table>
